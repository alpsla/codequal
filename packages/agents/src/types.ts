// Re-export core types for backward compatibility
// export { AgentProvider, AgentRole } from '../utils';

// Temporary type definitions
export type AgentProvider = string;
export type AgentRole = string;